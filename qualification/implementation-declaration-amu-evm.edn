;; kototama.vm/implementation-profile-v1 declaration - amu EVM backend slice.
{:schema :kototama.vm/implementation-profile-v1
 :spec :kototama-vm-v1
 :implementation :amu-evm256-kotoba-v1
 :version "1.0.0"
 :as-of "2026-09-05"
 :source-repo "kotoba-lang/amu"
 :source-sha "41a51d508f293ae601ac50e1b5144e09f23c727c"
 
 :profiles
 [{:profile :evm/v1
   :status :partial
   :levels [:bytecode]
   :omissions
   [;; admitted-main restricts to pure zero-arity main: no CALLDATA, no
    ;; env/block/tx context opcodes, no caller/address state access
    :message-level
    ;; only ADD (0x01) and MUL (0x02) arithmetic are emitted; no comparison,
    ;; bitwise, memory (MSTORE/MLOAD), storage (SSTORE/SLOAD), control-flow
    ;; (JUMPI), call family, or precompile opcodes
    :opcode-coverage
    ;; creation+runtime bytecode layout only; no gas accounting, no
    ;; execution against a state trie, no post-state root computation
    :state-level
    ;; no gas metering of any kind; spec :gas mapping and
    ;; :ethereum-gas-equality false are unexercised
    :gas-metering
    ;; :ethereum-general-state-tests and :differential-execution suites are
    ;; NOT run; required-suites for :evm/v1 are unmet
    :conformance-suites
    ;; no deploy/creation flow, only artifact verification of emitted bytes
    :deployment
    ;; no keccak256/address/chain pins; :pins list unexercised
    :pins-unexercised]}]
 
 :non-claims
 [:semantic :state :gas-accuracy :ethereum-gas-equality :full-opcode-set
  :revm-parity :mainnet-deployability]
 
 :evidence
 {:kind :source-audit
  :method "manual read of src/kotoba/compiler/backend/evm.cljc at :source-sha; emitted-bytecode-level only (creation-bytes/runtime-bytes/verify-artifact!); JVM-free compile path amu compile --target evm256-kotoba-v1 landed in amu PR #787 (main 34450a1dc6)"
  :tests "amu test suite green at :source-sha; no EVM conformance suite exists yet"
  :limitation "This declaration asserts SHAPE conformity at the bytecode level only. Per spec :conformance, :evidence-required-for includes :partial - this file is that evidence, and it must be re-issued against a new :source-sha whenever the backend changes."}}
