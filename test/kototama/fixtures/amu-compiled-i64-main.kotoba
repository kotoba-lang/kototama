;; Provenance for amu-compiled-i64-main.wasm.
;; Compiled via kotoba-lang/amu (`compile-source` :wasm32-kotoba-v1), not
;; the legacy `kotoba wasm emit` path the other fixtures in this directory
;; use. Host-free: no actor:host imports.
(ns demo-amu-i64-main)

(defn main []
  42)
